export const sendVerificationEmail = async (email: string, token: string) => {
  // TODO: Implement actual email sending via nodemailer or external service
  console.log(`Send verification email to ${email} with token: ${token}`);
};
